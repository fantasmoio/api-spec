schemas:
  E57:
    description: A compact, vendor-neutral format for storing point clouds, images, and metadata produced by 3D imaging systems, such as laser scanners.
    type: object
    required:
      - file
    properties:
      file:
        $ref: 'https://github.com/fantasmoio/api-spec/components/File.yaml'

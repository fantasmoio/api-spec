schemas:
  Floorplan:
    type: object
    required:
      - file
      - name
      - level
    properties:
      file:
          "$ref": "#/definitions/File"
      level: #The logical floor of the building.
      name: 

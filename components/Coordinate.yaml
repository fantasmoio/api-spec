schemas:
  Coordinate: # The geographical coordinate information.
    type: object
    required:
      - latitude
      - longitude
    properties:
      latitude:
        type: string
      longitude:
        type: string

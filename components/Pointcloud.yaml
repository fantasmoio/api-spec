schemas:
  Pointcloud:
    type: object
    required:
      - file
    properties:
      file:
        $ref: 'https://github.com/fantasmoio/api-spec/components/File.yaml'
      globalPose:
        $ref: 'https://github.com/fantasmoio/api-spec/components/Pose.yaml'
      potreeUri:

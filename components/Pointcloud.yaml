schemas:  
  Pointcloud:
    type: object
    required:
      - file
    properties:
      file:
        "$ref": "#/definitions/File"
      globalPose:
        "$ref": "#/definitions/Pose"
      potreeUri:
